<div>
  <h1>Product Details</h1>
  <img src="{{product.image}}" alt="" />
  <p>{{product.title}}</p>
  <p>${{product.price}}</p>
  <p>{{product.description}}</p>

  <form action="/products/{{product._id}}/carts" method="POST">
    <button type="submit">Add to Cart</button>
  </form>

</div>